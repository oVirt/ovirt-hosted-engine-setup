# generated by ovirt-hosted-engine-setup
BOOTPROTO=none
DEVICE=eth0
HWADDR={{ VM_MAC_ADDR }}
ONBOOT=yes
TYPE=Ethernet
USERCTL=no
ZONE=public
IPADDR={{ VM_IP_ADDR }}
PREFIX={{ VM_IP_PREFIX }}
GATEWAY={{ GATEWAY }}
{% for d in DNS_ADDR %}
DNS{{loop.index}}={{ d }}
{% endfor %}
DEFROUTE=yes
IPV4_FAILURE_FATAL=no